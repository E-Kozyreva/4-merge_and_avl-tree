class ShowButtons:
    def merge_algo(t_way_button, f_way_button, e_way_button, back_button):
        t_way_button.show()
        f_way_button.show()
        e_way_button.show()
        back_button.show()
        return t_way_button, f_way_button, e_way_button, back_button
    

    def merge_algo_back(merge_button, treee_button, exit_button):
        merge_button.show()
        treee_button.show()
        exit_button.show()
        return merge_button, treee_button, exit_button
    
    
    def tfe_way(random_array_button, add_array_from_file_button, back_button):
        random_array_button.show()
        add_array_from_file_button.show()
        back_button.show()
        return random_array_button, add_array_from_file_button, back_button
    
    
    def tfe_way_back(t_way_button, f_way_button, e_way_button, back_button):
        t_way_button.show()
        f_way_button.show()
        e_way_button.show()
        back_button.show()
        return t_way_button, f_way_button, e_way_button, back_button
    
    
    def tree_algo(avl_tree_button, back_button):
        avl_tree_button.show()
        back_button.show()
        return avl_tree_button, back_button
    

    def tree_algo_back(merge_button, tree_button, exit_button):
        merge_button.show()
        tree_button.show()
        exit_button.show()
        return merge_button, tree_button, exit_button
    
    
    def avl_tree(random_array_button, add_array_from_file_button, back_button):
        random_array_button.show()
        add_array_from_file_button.show()
        back_button.show()
        return random_array_button, add_array_from_file_button, back_button
    
    
    def avl_tree_back(avl_tree_button, back_button):
        avl_tree_button.show()
        back_button.show()
        return avl_tree_button, back_button
    

    def generate_array(back_button):
        back_button.show()
        return back_button
    

    def generate_array_back(random_array_button, add_array_from_file_button, back_button):
        random_array_button.show()
        add_array_from_file_button.show()
        back_button.show()
        return random_array_button, add_array_from_file_button, back_button


class HideButtons:
    def merge_algo(merge_button, treee_button, exite_button):
        merge_button.hide()
        treee_button.hide()
        exite_button.hide()
        return merge_button, treee_button, exite_button
    

    def merge_algo_back(t_way_button, f_way_button, e_way_button, back_button):
        t_way_button.hide()
        f_way_button.hide()
        e_way_button.hide()
        back_button.hide()
        return t_way_button, f_way_button, e_way_button, back_button
    
    
    def tfe_way(t_way_button, f_way_button, e_way_button, back_button):
        t_way_button.hide()
        f_way_button.hide()
        e_way_button.hide()
        back_button.hide()
        return t_way_button, f_way_button, e_way_button, back_button
    
    
    def tfe_way_back(random_array_button, add_array_from_file_button, back_button):
        random_array_button.hide()
        add_array_from_file_button.hide()
        back_button.hide()
        return random_array_button, add_array_from_file_button, back_button
    
    
    def tree_algo(merge_button, tree_button, exit_button):
        merge_button.hide()
        tree_button.hide()
        exit_button.hide()
        return merge_button, tree_button, exit_button
    
    
    def tree_algo_back(avl_button, back_button):
        avl_button.hide()
        back_button.hide()
        return avl_button, back_button
    
    
    def avl_tree(avl_tree_button, back_button):
        avl_tree_button.hide()
        back_button.hide()
        return avl_tree_button, back_button
    
    
    def avl_tree_back(random_array_button, add_array_from_file_button, back_button):
        random_array_button.hide()
        add_array_from_file_button.hide()
        back_button.hide()
        return random_array_button, add_array_from_file_button, back_button
    

    def generate_array(random_array_button, add_array_from_file_button, back_button):
        random_array_button.hide()
        add_array_from_file_button.hide()
        back_button.hide()
        return random_array_button, add_array_from_file_button, back_button
    

    def generate_array_back(back_button, array_txt_button):
        back_button.hide()
        array_txt_button.hide()
        return back_button, array_txt_button